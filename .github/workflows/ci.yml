name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: python -m pip install uv

      - name: Sync project environment
        run: |
          uv venv .venv --python 3.12 --clear
          UV_PROJECT_ENVIRONMENT=.venv uv sync

      - name: Run regression suite
        run: .venv/bin/python -m unittest discover -s opensignal_its/tests -p 'test_*.py'

      - name: Run full Reflex boot smoke
        run: .venv/bin/python scripts/reflex_boot_smoke.py --frontend-port 3002 --backend-port 8001